# Workspace root (optional)
BRANDING_WS_ROOT=/tmp/branding_workspace

# CORS helper (optional)
PREVIEW_FRONTEND_ORIGIN=http://localhost:3000
CORS_EXTRA_ORIGINS=http://localhost:3000

# Detection configuration (optional)
# DETECTOR can be: template | orb | hybrid
DETECTOR=hybrid
# QUALITY can be: fast | balanced | best
QUALITY=balanced
